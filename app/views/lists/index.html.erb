<div class="container mt-5">
  <h1>Lists</h1>
  <% @lists.each do |list| %>
  <ul>
    <%= link_to list.name , list_path(list) %>
  </ul>
  <% end %>

  <i class="fas fa-plus"></i> <%=  link_to 'Add a new list' , new_list_path%>
</div>
